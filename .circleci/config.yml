version: 2.1

orbs:
  aws-ecr: circleci/aws-ecr@8.1.2

jobs:
  - aws-ecr/build-and-push-image:
    repo: "${AWS_RESOURCE_NAME_PREFIX}"
    tag: "${CIRCLE_SHA1}"

workflows:
  version: 2
  primary:
    jobs:
      - buildAndPushImage